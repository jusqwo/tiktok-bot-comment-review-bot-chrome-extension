// Where the Bouncer service runs. For a published extension, deploy service/server.mjs and put its
// https:// URL here. The panel URL parameter ?svc= overrides it (used by the tests).
export const SERVICE_URL = 'http://127.0.0.1:8787';
